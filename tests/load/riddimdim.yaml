phantom:
  address: localhost:8082
  ssl: false
  load_profile:
    load_type: rps # schedule load by defining requests per second
    schedule: line(10, 80, 3m)
  ammo_type: uripost
console:
  enabled: true
telegraf:
  enabled: false
